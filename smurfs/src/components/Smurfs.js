import React, { Component } from 'react';
import { connect } from "react-redux";

class Smurfs extends Component {
  render() {
    return (
      <div>
        
      </div>
    )
  }
};

const mapStateToProps = store => ({

})

export default (mapStateToProps, {/*imported action get smurfs */})(Smurfs)
